<!-- Trade Hustle Header -->
<header class="th-header">
  <div class="th-header-inner">

    <!-- Left: Hamburger -->
    <div class="th-left">
      <button class="menu-toggle" aria-label="Open menu">&#9776;</button>
    </div>

    <!-- Center: Logo -->
    <div class="th-center">
      <img 
        src="{{ 'trade-hustle-logo.png' | asset_url }}" 
        alt="Trade Hustle Logo" 
        class="th-logo">
    </div>

    <!-- Right: Search -->
    <div class="th-right">
      <form action="/search" method="get" role="search">
        <input 
          type="search" 
          name="q" 
          placeholder="Search..." 
          class="th-search" 
          aria-label="Search">
      </form>
    </div>
  </div>

  <!-- Hero Titles -->
  <div class="th-hero-title">
    <h1 class="main-title">WELCOME TO THE GRIND</h1>
    <p class="sub-title">Built for the Trade. Backed by Hustle.</p>
  </div>
</header>

<style>
/* ===========================
   HEADER STYLING
   =========================== */
:root {
  --hustle-red: #E10600;
  --hustle-yellow: #FFD700;
  --electric-blue: #0099FF;
  --jet-black: #000000;
  --white: #FFFFFF;
}

.th-header {
  background: var(--jet-black);
  padding: 10px 0 30px 0;
  position: relative;
  z-index: 1000;
}

.th-header-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
}

.th-left,
.th-right {
  flex: 1;
  display: flex;
  align-items: center;
}

.th-left {
  justify-content: flex-start;
}

.th-right {
  justify-content: flex-end;
}

.th-center {
  flex: 0 0 auto;
  display: flex;
  justify-content: center;
}

.th-logo {
  height: 70px;
  display: block;
  margin: 0 auto;
}

/* Hamburger */
.menu-toggle {
  font-size: 26px;
  color: var(--hustle-yellow);
  background: none;
  border: none;
  cursor: pointer;
}

/* Search bar */
.th-search {
  padding: 6px 10px;
  border-radius: 4px;
  border: 1px solid var(--electric-blue);
  background: var(--jet-black);
  color: var(--white);
  font-size: 14px;
}
.th-search::placeholder {
  color: #ccc;
}

/* ===========================
   TITLES
   =========================== */
.th-hero-title {
  text-align: center;
  margin-top: 20px;
}

.main-title {
  font-family: 'Anton', sans-serif;
  font-size: 42px;
  font-weight: 700;
  color: var(--hustle-yellow);
  text-shadow: 0 0 10px var(--hustle-red), 0 0 20px var(--hustle-red);
  letter-spacing: 2px;
  margin-bottom: 8px;
}

.sub-title {
  font-family: 'Garamond', serif;
  font-size: 20px;
  color: var(--electric-blue);
  text-shadow: 0 0 8px var(--jet-black);
  letter-spacing: 1px;
  margin: 0;
}

/* MOBILE */
@media (max-width: 768px) {
  .th-right {
    display: none;
  }
  .th-logo {
    height: 55px;
  }
  .main-title {
    font-size: 32px;
  }
  .sub-title {
    font-size: 16px;
  }
}
</style>

